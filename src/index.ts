import './styles.css';

import { Button as CButton } from './components/Button';
import { Modal as CModal } from './components/Modal';
import { Checkbox as CCheckbox } from './components/Checkbox';
import { Select as CSelect } from './components/Select';
import { Switch as CSwitch } from './components/Switch';
import { TextField as CTextField } from './components/TextField';

export const Button = CButton;
export const Modal = CModal;
export const Checkbox = CCheckbox;
export const Select = CSelect;
export const Switch = CSwitch;
export const TextField = CTextField;

export default { Button, Modal, Checkbox, Select, Switch, TextField };
